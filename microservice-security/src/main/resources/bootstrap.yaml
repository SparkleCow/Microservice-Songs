spring:
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
  application:
    name: msvc-auth

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html
  packages-to-scan:
  paths-to-match:
    - /api/v1/**